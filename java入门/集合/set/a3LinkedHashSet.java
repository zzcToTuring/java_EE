package 集合.set;

public class a3LinkedHashSet {
    public static void main(String[] args) {
        /**
         * 基本概述：有序(保证存储和取出的元素顺序一致)，不重复，无索引
         * 原理：底层数据结构依然是哈希表，只是每个元素又额外多了一个双链表的机制记录存储的顺序（基于底层哈希表，使用双链表记录添加顺序）
         * 特点
         */
    }
}
